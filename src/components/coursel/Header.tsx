import { FC } from 'react';

const Header: FC = () => (
  <header>
    <h1 className="pb-3 text-center text-2xl font-bold capitalize text-cyan-600 lg:text-4xl">
      les miellers offres
    </h1>
  </header>
);

export default Header;
